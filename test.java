class Solution {
    public int maxProfit(int[] prices) {

        for (int i = 0; i < prices.length; i++)
        // running through the array
        {
            while (prices != null)
            // as long as its not the end of the array do this loop
            {
                if (prices[i] < (prices[i] + 1))
            // if the current is less than the next one then i wanna save it
                {
                    
                }
            }
        }
        return prices;
    }
}